# ── Foodies Pakistan — FastAPI Microservice ──
# General-purpose Python backend at fastapi.foodiespakistan.pk

# Core Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Template Engine
jinja2==3.1.4
aiofiles==24.1.0       # For static file serving

# VR Tour Module
# NOTE: 360° equirectangular stitching uses Hugin / panotools — a SYSTEM package
#       (NOT pip). Install it on the host before running:
#         sudo apt install -y hugin-tools enblend
#       Binaries used: pto_gen, cpfind, cpclean, linefind, autooptimiser,
#                      pano_modify, nona, enblend
opencv-python-headless==4.10.0.84   # Post-processing: read blended TIFF → WebP + thumbnail
numpy==1.26.4                        # Array ops in stitcher post-processing
Pillow==11.0.0                       # Image utilities

# HTTP Client (for CDN uploads & Node.js API calls)
httpx==0.28.1

# File Upload Handling
python-multipart==0.0.18

# Environment
python-dotenv==1.0.1
